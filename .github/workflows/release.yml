name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  extract-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - name: Extract version from tag
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

  build-unix:
    needs: extract-version
    uses: ./.github/workflows/build-macos-pkg.yml
    with:
      version: ${{ needs.extract-version.outputs.version }}
    secrets: inherit

  build-windows:
    needs: extract-version
    uses: ./.github/workflows/build-windows.yml
    with:
      version: ${{ needs.extract-version.outputs.version }}
    secrets: inherit

  release:
    needs: [extract-version, build-unix, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        id: checksums
        run: |
          mv artifacts/safechain-agent-darwin-amd64/safechain-agent-darwin-amd64 .
          mv artifacts/safechain-agent-darwin-arm64/safechain-agent-darwin-arm64 .

          mv artifacts/safechain-proxy-darwin-amd64/safechain-proxy-darwin-amd64 .
          mv artifacts/safechain-proxy-darwin-arm64/safechain-proxy-darwin-arm64 .

          mv artifacts/safechain-proxy-linux-amd64/safechain-proxy-linux-amd64 .
          mv artifacts/safechain-proxy-linux-arm64/safechain-proxy-linux-arm64 .

          mv artifacts/SafeChainAgent.amd64.msi/SafeChainAgent.amd64.msi ./SafeChainAgent-windows-amd64.msi
          mv artifacts/SafeChainAgent.arm64.msi/SafeChainAgent.arm64.msi ./SafeChainAgent-windows-arm64.msi

          mv artifacts/SafeChainAgent-amd64.pkg/SafeChainAgent-amd64.pkg ./SafeChainAgent-darwin-amd64.pkg
          mv artifacts/SafeChainAgent-arm64.pkg/SafeChainAgent-arm64.pkg ./SafeChainAgent-darwin-arm64.pkg

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            safechain-agent-darwin-amd64
            safechain-agent-darwin-arm64
            safechain-agent-ui-darwin-amd64
            safechain-agent-ui-darwin-arm64
            safechain-proxy-darwin-amd64
            safechain-proxy-darwin-arm64
            safechain-proxy-linux-amd64
            safechain-proxy-linux-arm64
            SafeChainAgent-windows-amd64.msi
            SafeChainAgent-windows-arm64.msi
            SafeChainAgent-darwin-amd64.pkg
            SafeChainAgent-darwin-arm64.pkg

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
